[package]
name = "nss-oidc-direct-grant"
version = "0.1.0"
edition = "2024"
description = "NSS module to authenticate via OIDC direct grant."
publish.workspace = true
authors.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
